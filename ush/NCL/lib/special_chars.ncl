;
; **********************************************************************
;
; Define special characters that can't be directly input into an NCL 
; string.
;
; **********************************************************************
;
char_nl = str_get_nl()
char_dq = str_get_dq()
char_sq = str_get_sq()
